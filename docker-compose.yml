services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CLAUDE_MODEL=${CLAUDE_MODEL:-claude-sonnet-4-6}
      - DATABASE_PATH=/app/data/banking.db
    volumes:
      - db_data:/app/data

volumes:
  db_data:
